/**
 * Plugin constants (hardcoded configuration) for shared code.
 */
export default class Gtm_Mob_Shared_Defaults {
    NAME = '@flancer32/gtm-mobile';

    constructor() {
        Object.freeze(this);
    }
}
